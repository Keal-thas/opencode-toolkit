# mcp-servers/ backlog

**All of these should be built as `type: "remote"` MCP servers (a persistent HTTP process opencode connects to) from the start, not `type: "local"` (opencode-spawned over stdio)** — `oracle/` was originally built local/stdio and converted to remote/HTTP later (2026-09-14, see `mcp-servers/oracle/README.md`'s Design and Status sections for why and how); the later servers below shouldn't repeat that detour. Reuse `oracle/server.js`'s pattern: Node's built-in `http` plus `@modelcontextprotocol/sdk`'s `StreamableHTTPServerTransport` in stateless mode (`sessionIdGenerator: undefined`, a fresh `Server`/transport pair per request), no extra HTTP framework dependency needed.

- [x] **Loki** — done 2026-09-14. `mcp-servers/loki/` exposes `loki_query_range`/`loki_labels`/`loki_label_values` against the Loki HTTP query API (just `fetch`, no driver dependency, as planned), `type: "remote"` from the start. Tested end-to-end against a real Loki instance in the docker sandbox (`docker/docker-compose.loki.yml`) — see `mcp-servers/loki/README.md`'s Status section and `docs/feature-points/14-loki-mcp-server.md`.
- [x] **java-lsp / spring-lsp** — done 2026-09-18. Not internal-ops LAN tooling like the four above (Oracle/Loki/Redis/MySQL) — these two spawn and drive a real LSP server (jdtls, and VMware's `spring-boot-language-server`) over stdio instead of hitting an HTTP API, but still `type: "remote"` MCP servers per this file's own rule, for the same reason: LSP is a genuinely stateful session (project indexing, incremental document sync), so `server.js` keeps one LSP-client singleton alive for the server's whole lifetime rather than per-request, while the outer MCP/HTTP layer stays stateless-per-request like `oracle`/`loki`. Built because opencode's own built-in `jdtls` LSP integration only auto-detects `java` on `PATH` with no way to point it at a different JDK for the server process itself (see the session notes this was built from, and both packages' READMEs' "JDK version" sections). `lsp-client.js` (the shared framing/handshake/document-sync engine) is duplicated verbatim between the two packages rather than pulled in as a shared dependency — see `mcp-servers/java-lsp/README.md`'s Design section for why. Both verified end-to-end against real, real language-server processes (jdtls 1.61.0 via `brew install jdtls`; the vendored `spring-boot-language-server` 2.5.0-SNAPSHOT, extracted from VMware's VS Code extension since it isn't published to any package registry — see `mcp-servers/spring-lsp/README.md`'s Vendoring section, including its real ~81MB size, by far the largest binary this repo has committed). `java-lsp` is fully verified with rich results (real symbol resolution, hover, references, definitions) against a plain Java file. `spring-lsp` is only verified at the protocol/plumbing level — its Spring-specific richness (bean graph, config-property completion) needs either a real Maven/Gradle project with resolved dependencies, or pairing with a companion jdtls via the "classpath listener" mechanism VS Code's Java+Spring extensions use together — neither was set up here, see `mcp-servers/spring-lsp/README.md`'s Status section for the exact failure mode (empty results, not errors) and what's needed to fix it. Wired into `deploy/opencode.json.example` (`enabled: true`, ports 8092/8093).
- [ ] **java-lsp / spring-lsp: wire the two together via the classpath-listener mechanism** so `spring-lsp`'s tools get real classpath-aware results instead of timing out to empty ones (see `mcp-servers/spring-lsp/README.md`'s Status section for the exact `SpringSymbolIndex`/`JdtLsProjectCache` timeout this would fix). Not attempted — the mechanism VS Code's Java+Spring extensions use to talk to each other isn't publicly documented; would need to be reverse-engineered from `sts4`'s and `redhat.java`'s source.
- [ ] **java-lsp / spring-lsp: not wired into `tests/run-in-container.sh` / the docker/ sandbox.** The sandbox's image (`docker/Dockerfile`, `FROM node:22-bookworm`) has no JDK or jdtls installed; adding one (and deciding how jdtls itself gets into the image — `apt`, a vendored download, etc.) wasn't done as part of building these two packages and would need its own sign-off, same as any new download source added to the committed pipeline. Both packages' tests (`java-lsp.test.mjs`/`spring-lsp.test.mjs`) run fine standalone on a machine with the right JDK/jdtls installed; they just aren't part of `./tests/run-all.sh` yet.
- [ ] **java-lsp / spring-lsp: only tested against a single loose `.java` file, not a real Maven/Gradle multi-file project.** jdtls's/spring-boot-language-server's cross-file resolution across a real dependency graph should work the same way in principle, but wasn't specifically exercised.
- [ ] **java-lsp: `jdtls` itself has no supply path onto the restricted target machine yet — SETUP.md was deliberately not given a step 8 for this.** Unlike `mcp-servers/oracle`'s/`mcp-servers/loki`'s npm dependencies (resolved from the restricted machine's internal npm mirror) or `spring-lsp`'s vendored jar (committed to this repo, travels with it), `jdtls` is a whole Eclipse JDT LS distribution, not an npm package — nothing about how it should reach that machine (vendored tarball like `spring-lsp`'s, the internal npm mirror if it somehow also mirrors non-npm downloads, some other channel) was decided or verified here. `java-lsp/README.md`'s own dev-verification used `brew install jdtls` on macOS, which has no equivalent on the restricted Windows/git-bash machine.
- [ ] `loki/`'s relative/naive time-param resolution (`resolveTimeParam()` in `mcp-servers/loki/server.js`, added 2026-09-15 for agent-friendliness - see `mcp-servers/loki/README.md`) - `now`/`now-<duration>`/naive-local-time parsing was verified standalone (node -e sanity check, not committed as a test) but not yet exercised against a real Loki instance or covered by `mcp-servers/loki/loki.test.mjs`. Add cases there and re-run `./tests/run-all.sh` before relying on this in production.
- [ ] `oracle/`'s `auditQuery()` hook — still a no-op by design (see `mcp-servers/oracle/README.md`'s design section for why); not urgent, do it later. Rule-based (regex/keyword denylist) or LLM-based (mirroring `plugins/llm-review-gate/llm-review-gate.ts`), or both — undecided.
- [ ] `oracle/`'s `executeQuery()` has no result-size cap and issues no session-level statements before running the agent's SQL. Worth adding together: (1) a `maxRows`-based cap on `connection.execute()` (fetch `N+1`, trim to `N`, return a `truncated: true` flag when exceeded — driver-level, so it applies regardless of the SQL's shape, unlike trying to inject `FETCH FIRST`/`LIMIT` into arbitrary agent-written SQL text — cap configurable via an env var), since right now a broad `SELECT` against a large table fetches and JSON-serializes every row into the tool response with no limit; (2) `SET TRANSACTION READ ONLY` issued right after `getConnection()`, before the agent's SQL runs — verified live against the sandbox's `oracle` instance (2026-09-21) to reject DML (`ORA-01456`) even for an account that genuinely holds INSERT/UPDATE/DELETE grants, so this is a real independent layer on top of whatever `ORACLE_USER` happens to have, not just a restatement of the account's own grants; (3) `ALTER SESSION SET CURRENT_SCHEMA` support, confirmed wanted at two config levels (2026-09-21, Franco, high confidence): a project/deployment-level default (an `ORACLE_DEFAULT_SCHEMA` env var, applied on every connection if set) plus a user/call-level override (an optional `schema` argument on the `oracle_query` tool itself, so one deployment isn't locked to a single schema if a particular query needs a different one) — so agents don't have to qualify every table name with `schema.table`; (4) `ALTER SESSION SET NLS_DATE_FORMAT` for consistent date rendering — an ergonomics nicety, not a safety measure. Doesn't replace using a read-only DB account (see `mcp-servers/oracle/README.md`'s Recommended read-only account section) — that stays the primary boundary; this is defense-in-depth plus making the tool more agent-friendly out of the box.
- [x] **Publish `oracle/` and `loki/` as independently-versioned npm packages** — code-side changes done and both bootstrap versions live on npm 2026-09-21 (`@kealthas-dev/opencode-mcp-oracle@1.0.0`, `@kealthas-dev/opencode-mcp-loki@1.0.0`). Mirrors `plugins/*`'s existing pattern so updating either server on the restricted machine is now `npm install -g <pkg>@latest` instead of re-transferring the whole repo and re-running `cp -r` + local `npm install`. Each server's `package.json` dropped `"private": true` and gained `"publishConfig": {"access": "public"}`, a `"repository"` block, and a `"bin"` entry pointing at `server.js` (which got a `#!/usr/bin/env node` shebang) so a global install drops a runnable command on `PATH`. `.github/workflows/publish-plugins.yml` was renamed to `publish-packages.yml` and its directory loop/`paths:` trigger extended to include both, rather than adding a near-duplicate parallel workflow — this is what future version bumps publish through automatically. SETUP.md steps 6/7 and both READMEs' Run sections now describe the global-install path as primary; the local `npm install && npm start` path stays for the Docker-sandbox/dev flow, which is unaffected. `java-lsp`/`spring-lsp` stay `private: true` and out of scope, per the original plan.
  - **Still needed, one-time, on npmjs.com (Franco):** bind each package's Settings → Trusted Publisher to the `publish-packages.yml` workflow, same as the three `plugins/*` packages already have — until that's done, future version bumps won't auto-publish via CI (the bootstrap publish above used Franco's own npm login, not OIDC).
  - The bootstrap publish itself needed a real account login — the pre-existing automation token in this machine's `~/.npmrc` (scoped to the already-published `plugins/*` packages only) couldn't create these two brand-new package names (`npm publish` failed `404`, `npm access`/`npm whoami` failed `401`), confirming npm Trusted Publishing's own constraint: a package's Settings page (where Trusted Publisher gets configured) doesn't exist until the package has been published at least once by a real logged-in account.
- [x] `oracle/`/`loki/` offline dependency install — was tracked here as blocked (no way to `npm install` `@modelcontextprotocol/sdk`/`oracledb` on the offline target machine). Resolved 2026-09-17: the target machine, while lacking public internet, has a working internal npm registry (confirmed by Franco, a full mirror of public npm, not a curated subset) — so a plain `npm install` in SETUP.md steps 6/7 resolves these directly, no vendoring needed.
- [ ] Redis — official `redis/mcp-redis` exists but defaults to read/write; would need trimming to a read-only tool set (or an ACL-based read-only Redis user) before use here.
- [ ] MySQL — several community MCP servers exist; pick one that's actually read-only at the SQL layer, not just by convention, before adopting.
- [ ] xxl-job — no existing implementation anywhere (confirmed via [xuxueli/xxl-job#3775](https://github.com/xuxueli/xxl-job/issues/3775), open since 2025-08-11, unresolved). Would mean reading xxl-job-admin's internal AJAX endpoints directly, no public API docs to go from. Read-only (job/log status) only — never expose trigger/kill.

Secondary candidates raised but not yet worth their own line above: Nacos/Apollo (config center, read-only), RocketMQ/Kafka (topic lag / consumer offsets), SkyWalking/Zipkin (trace lookup), Jenkins/GitLab CI (build status) — all fit the same "internal-ops, read-only, one server per LAN system" shape as the four above.

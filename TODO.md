# TODO

Concrete, actionable follow-up work for this repo. Different from README.md's "Status / open items" (known limitations/facts about the current setup) — this file is things that should actually get done. Remove an item once it's done instead of leaving it checked off.

- **opencode web UI spacing/styling complaint (raised 2026-09-26, Franco) — not started, only scoped.** `opencode web`/`serve` feels too spaced-out. Investigated how to change it without rebuilding opencode: the served UI (`packages/app` in `anomalyco/opencode`, Solid + Tailwind, shared design tokens from `packages/ui`) is compiled and embedded as raw bytes directly inside the platform-specific binary (`opencode-<platform>-<arch>/bin/opencode`) via Bun's `import ... with { type: "file" }` at build time — there's no loose `dist/*.css` file sitting in a real `npm install -g opencode-ai` tree to hand-edit. The one related env var, `OPENCODE_DISABLE_EMBEDDED_WEB_UI`, only turns the embedded UI off entirely (falls back to proxying `https://app.opencode.ai`, unreachable from the offline target machine anyway) — it doesn't redirect to a custom local build. See `docs/lessons-learned.md` for the full trace. **Next step, not yet done:** open the real `opencode web` UI (in a browser, e.g. this session's browser pane) to identify the actual Tailwind classes/CSS custom properties responsible for the spacing, then write a small custom-CSS override applied client-side (a userstyle via something like the Stylus extension, or a browser's own local-overrides feature) — zero opencode rebuild, survives upgrades, fully reversible. Open question for Franco: is this worth doing at all given it only affects `web`/`serve` mode, not the CLI/TUI he mainly uses?

- **Oracle/Loki singleton first-time-startup race (deferred).** If two worktrees both race to bring up a shared Oracle/Loki container for the very first time ever on a machine (started manually now, not by `docker/dev.sh` — see `docker/docker-notes.md`), one `docker compose up -d` succeeds and the other gets a real, non-zero-exit `Conflict: container name already in use` error. Once the container is already running, later concurrent `up -d` calls from any worktree are fine (idempotent reconcile). Narrow window, low blast radius (just retry).

- **Docker image tag (`opencode-toolkit-dev:latest`) build race across worktrees (deferred).** The image is deliberately global/shared across all worktrees (rebuilding a multi-GB toolchain image per worktree would be wasteful) — so it sits outside the per-worktree isolation `docker/dev.sh` gives everything else. If two worktrees have different uncommitted `Dockerfile` content and both run `docker/dev.sh build` around the same time, whichever finishes last silently wins the `:latest` tag — the other's successful build just becomes untagged/dangling, and that worktree's next `docker/dev.sh run` quietly uses someone else's image instead of the one it just built, with no error.

- **opencode memory — behavioral/decision track done 2026-09-18, document-corpus/RAG track still not started.** Split into two unrelated problems after discussion with Franco (chat log 2026-09-18): small structured facts (preferences, decisions, project facts) vs. a future large reference-document corpus (e.g. requirement specs). The first is now built — see `docs/feature-points/15-opencode-memory-mcp.md` (the official `@modelcontextprotocol/server-memory` package, wired via `deploy/opencode.json.example`'s `mcp.memory` entry + `SETUP.md` step 10 + `deploy/system-prompt.txt`'s `# Memory` section). The second — semantic search over a document corpus too big to fit in context (deployed context confirmed by Franco as 128K, not the model's native 262K–1M, so this has a real ceiling) — is untouched: needs a chunking/ingestion pipeline, an embedding model, and a vector store, none of which exist here yet, and the model server's own embedding capability is unconfirmed (Qwen3.6-35B-A3B is a generative chat model, not built for embeddings, and there's no evidence the vLLM deployment exposes `/v1/embeddings` for it). If this becomes worth doing: try full-text search over the raw files first (SQLite FTS5 or ripgrep, zero new dependencies) before reaching for vector search; if vector search does turn out to be needed, the leading candidate found so far is Qdrant (self-hosted vector DB with native sparse+dense hybrid support) plus a separately-run local embedding model (e.g. via Ollama, decoupled from the vLLM Qwen deployment entirely) — but note the official `qdrant/mcp-server-qdrant` MCP server only does plain dense search (no hybrid) and is a Python/`uv` package, a different runtime stack than this workspace's existing Node-based MCP tooling — both would need hands-on verification on the actual target machine before committing to them, not just documentation review.

- **Extract recurring opencode workflows into `SKILL.md` files (raised 2026-09-21, Franco).** No `SKILL.md` exists anywhere in this repo yet — opencode itself ships no built-in skills either, the `skill` mechanism is pure user/repo-defined discovery (see `docs/opencode-docs-reference/skills.mdx`). Franco has flagged that some workflows he repeats often are worth distilling into skills, but hasn't named specific candidates yet — revisit once he does. One candidate raised in discussion: a SQL descriptive-statistics skill for the Oracle MCP server (common aggregate-query patterns, how to explore schema structure first via `dba_tables`/`dba_source` before writing queries) — see `mcp-servers/oracle/README.md`'s Recommended read-only account section for the account this would query against.

- **Possible new opencode plugin custom tool (raised 2026-09-21, Franco).** Undecided — no concrete use case identified yet. None of the three existing plugins (`system-prompt-tools`/`hook-logger`/`llm-review-gate`) register a custom `tool` today (confirmed by reading all three source files — each only returns hook handlers, no `tool` key, and none imports the `tool` helper from `@opencode-ai/plugin`), so this would be a new pattern for this repo's `plugins/`, not an extension of an existing one. Revisit once there's an actual gap a plugin-registered tool would fill better than an MCP server tool (the two aren't interchangeable — see `docs/opencode-docs-reference/plugins.mdx`'s Custom tools example for the shape).

- **Open decisions on the memory MCP feature that Franco should sanity-check, not things I was unsure how to resolve — flagging per his instruction to leave decisions in docs rather than ask mid-session:**
  - `MEMORY_FILE_PATH` set to `$CONFIG_DIR/memory.jsonl` — local machine state, not backed up or version-controlled by anything in this repo, unlike this repo's own git-tracked `memory/`. If the accumulated knowledge graph should survive a machine wipe/reinstall, that needs an explicit backup step this repo doesn't currently provide.
  - `deploy/system-prompt.txt`'s `# Memory` section wording is a first draft, untested against the real Qwen3.6-35B-A3B model's actual tool-calling behavior (see SETUP.md's "Report back" section, step 9) — may need retuning for over- or under-triggering once there's real usage to look at.
  - No forced retrieval at session start (relies on the model following the prompt instruction, not a harness-level guarantee) — a plugin `session.created` hook that always injects the graph would be stronger, closer to how Claude Code's own memory index is unconditionally loaded every conversation, but felt like scope beyond what was asked for a first cut. Worth revisiting if the prompt-only version proves unreliable.

- **oracle/loki integration tests lost their auto-bring-up — `tests/run-all.sh` isn't one-shot green anymore.** `dev.sh` now only starts opencode-dev (see `docker/docker-notes.md`); oracle/loki are separately-started shared fixtures. But the oracle/loki integration tests in `tests/run-all.sh` → `tests/run-in-container.sh` still assume both are up, so `run-all.sh` now fails those two tests unless oracle/loki were started first (`docker compose -f docker/docker-compose.oracle.yml up -d --wait` + `...loki.yml up -d`). Undecided: have `run-all.sh` bring the fixtures up itself (keeps one-shot green) vs accept the precondition and just document it. Also: two `tests/README.md` lines still say "`docker/dev.sh` always brings up first" and need fixing.

- **`gitbash-edit-path-fix.ts` plugin (added 2026-09-26) only covers the `edit` tool and is unverified against a real opencode run — several pieces still open:**
  - **`write` and `apply_patch` have the same class of bug, not yet fixed.** `write.ts` has the identical `path.isAbsolute(params.filePath) ? params.filePath : path.join(...)` branch as `edit.ts` — extending the plugin to it is a copy of the existing `edit` handling. `apply_patch.ts` is harder: the path isn't a discrete tool argument, it's embedded inside the patch text's `*** Update File: <path>` (and `*** Add File:`/`*** Delete File:`/`*** Move to:`) headers, so fixing it needs a small text rewrite of those header lines, not a single-field mutation. See `docs/feature-points/18-gitbash-edit-path-fix-plugin.md`.
  - **The underlying path-resolution bug is now confirmed on a real Windows kernel (2026-09-26), via a one-off GitHub Actions `windows-latest` run — see `docs/lessons-learned.md`.** `edit.ts`'s exact `path.isAbsolute(...) ? ... : path.join(...)` line, run for real: `/c/opencode-verify/marker.txt` resolved to and actually wrote `D:\c\opencode-verify\marker.txt`, never touching the intended `C:\opencode-verify\marker.txt`. This closes the "is the root cause even real on genuine Windows" question — what's still open below is narrower: whether *this specific plugin*, loaded into a *real opencode instance*, actually intercepts and fixes it end to end.
  - **Never actually run against a live opencode session, only unit-tested.** All 8 tests in `tests/unit/gitbash-edit-path-fix.test.mjs` pass, including one that reproduces `edit.ts`'s own `path.isAbsolute` line via `path.win32` — but they exercise the plugin's own logic in isolation, with `process.platform` force-overridden to `"win32"` (this sandbox's `node:22-bookworm` dev container is Linux and can never take that branch on its own). They do not prove opencode's real plugin loader actually invokes `tool.execute.before` the way `packages/opencode/src/session/tools.ts` source reading says it does, nor that a real model-issued `edit` call on the real Windows + Git Bash target machine ends up editing the intended file. The GitHub Actions run above proves the *bug*, not that *this plugin's hook* actually fires and fixes it inside a real opencode process — that gap needs either a real opencode run on Windows (a VM, or the offline target machine) or, cheaper, a similar one-off Actions job that installs `opencode-ai` + this plugin for real and drives one `edit` tool call end to end.
  - **This repo's Docker sandbox still can't close the remaining "does opencode's real plugin loader honor this hook" gap.** It's Linux (`process.platform` is never `"win32"` there), and a live-session smoke test via `opencode run` is separately blocked by the pre-existing, unrelated `opencode run` hang bug logged in `docs/lessons-learned.md` ("hangs indefinitely right after the `init` bootstrap log line") — the same doc also flags that `opencode debug config` was never fully confirmed to *execute* a configured plugin's hook bodies rather than just resolve/install its import graph.
  - **Not published to npm, not added to `deploy/opencode.json.example`'s `plugin` array yet** — deliberately held back until the point above is resolved; flipping that on for the real deployment before a real-machine check felt like the wrong default to pick unilaterally.
  - **Worth a second, skeptical look before trusting it further: the "mutate `output.args.filePath` in place, don't reassign `output.args`" claim.** Verified by reading `packages/opencode/src/session/tools.ts` (`output.args`/the `args` closure variable are the same object reference) and `packages/opencode/src/plugin/index.ts`'s `trigger()` (calls every plugin's hook against that one shared `output` object) — and it lines up with the still-open `anomalyco/opencode#42409` ("tool.execute.before hook: modifying output.args.command does not affect the executed command") looking like the same reassignment-vs-mutation mistake. But per this repo's own "verify against the real system" habit (`docs/lessons-learned.md`), reading the source isn't the same as watching it happen — worth confirming with a real gated `edit` call once the blockers above are cleared.
  - **A Windows-VM verification path was scoped out (2026-09-26) but shelved by Franco before starting — not attempted.** Discussion landed on VMware Fusion specifically (free for personal use, negligible perf difference from Parallels for ARM-native CLI tools like Git Bash/Node, no subscription-expiry risk for a throwaway VM) over Parallels (paid, only wins on setup speed/polish which doesn't matter for a disposable test box) and UTM (free but the most manual image-sourcing step). Also confirmed on this Mac beforehand: no `vmnet*` interfaces currently exist, so no subnet-collision risk from an existing hypervisor; Fusion's own networking (a System Extension on Apple Silicon, not a legacy kext) only adds interfaces, it doesn't touch this machine's existing `en*`/`bridge0`/`utun0-4`. Since the raw path-resolution question got answered more cheaply via a GitHub Actions run instead (see above), a VM is now only worth it for the narrower "real opencode + real plugin" pipeline check, not for re-confirming the underlying bug. No need to re-litigate the Fusion-vs-Parallels choice if picked back up.

- **Wire IntelliJ IDEA Ultimate's built-in MCP debugger toolset into opencode (raised 2026-09-24, Franco — confirmed IDEA Ultimate is present on the offline machine).** IDEA 2025.2+ ships a built-in MCP server (`Settings | Tools | MCP Server`) that, once its separate "Debugger MCP toolset" plugin is also enabled (a second toggle beyond the base MCP Server one), exposes live JVM debugging as MCP tools: breakpoints (`xdebug_set_breakpoint`/`_list_breakpoints`/`_remove_breakpoint`), session control (`xdebug_start_debugger_session`, `xdebug_control_session` for step/resume/pause/stop, `xdebug_run_to_line`), stack/variable inspection (`xdebug_get_stack`, `_get_frame_values`, `_get_value_by_path`, `xdebug_evaluate_expression`, `xdebug_set_variable`), and monitoring (`xdebug_get_debugger_status`, `_get_threads`). This is a capability class nothing else here covers — `mcp-servers/java-lsp`/`spring-lsp` are static analysis only (definitions/references/hover), no runtime control.
  - **Status: config stub added, awaiting Franco's on-machine test.** `deploy/opencode.json.example`'s `mcp` block now has an `idea` entry (`type: "remote"`, `enabled: false`, placeholder `url`) sitting next to `java-lsp`/`spring-lsp`. To activate it: on the offline machine, enable both IDEA toggles above, then in the same settings panel click "Copy HTTP Stream Config" (or SSE) to get the actual `localhost:<port>` URL for that running instance — not documented anywhere (JetBrains' help page is JS-rendered with no port/path disclosed), has to be read off the IDE locally. Paste it over the placeholder and flip `enabled` to `true`.
  - Open questions, to resolve once Franco has actually exercised this on the real machine:
    - Permission gating: `xdebug_evaluate_expression`/`xdebug_set_variable` execute arbitrary code and mutate state in a live JVM — a materially different risk class than the read-only `java-lsp`/`spring-lsp` tools, or `oracle`'s already-made full-passthrough decision. Needs its own explicit allow/ask call via opencode's per-tool-name permission wildcards (`"idea_xdebug_*": ...`), not a default inherited from another server.
    - Whether IDEA reuses a stable port across restarts, or the URL needs re-copying every relaunch — if it's unstable, the checked-in `url` value will go stale and this entry may need to move to a machine-local override instead of living directly in the tracked example.
    - This only works while IDEA Ultimate is open with the same project loaded — it doesn't fit `deploy/`'s headless CLI/`serve`/`web` usage pattern by itself, only interactive sessions where Franco has both opencode and the IDE open side by side.
    - `xdebug_start_debugger_session` launches "from a run configuration or code location" — for whichever Spring Boot app this targets, a working IDEA run/debug configuration needs to already exist; not yet confirmed one does.
